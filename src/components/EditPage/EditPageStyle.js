import {makeStyles} from "@material-ui/core/styles";

export default makeStyles ( () => ({
    input: {
        marginTop: "35px",
        marginBottom: "5px",
        width: "100%",
        border: "none",
        borderBottom: "1px solid #000",
    },
    error: {
        color: "red",
    },
    btn: {
        marginTop: "25px",
    }
}) )