import {makeStyles} from "@material-ui/core/styles";

export default makeStyles ( () => ({
    appBar: {
        height: "70px",
        display: 'flex',
        flexDirection: "row",
        alignItems: "center",
    },
    title: {
        paddingLeft: "25px",
        color: "#fff",
        textDecoration: "none",
    }
}) )